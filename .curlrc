# When following a redirect, automatically set the previous URL as referer
referer = ";auto"

#enable netrc support, to grab usernames and passwords
netrc

silent

show-error

header = "Accept: application/json, application/xml, text/plain, */*"
